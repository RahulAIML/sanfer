# Render (and any host) sets NODE_ENV=production, which makes npm skip
# devDependencies — but the build needs them: typescript, vite and the React
# type packages all live there. Install them regardless of NODE_ENV.
include=dev
